const strs = ["eat", "tea", "tan", "ate", "nat", "bat"];
const results = [["bat"], ["nat", "tan"], ["ate", "eat", "tea"]];

/**
 * Try 1
 */

function groupAnagrams(strs: string[]): string[][] {
  return [];
}
